@page
@model IndexModel
@{
    ViewData["Title"] = null;
}

<div class="text-center">
    <p> плейлисты:</p>
    @foreach (var playlist in Model.Playlists)
    {
        <p> название :@playlist.Name</p>
        <p> Автор: @playlist.AuthorName</p>
    }
</div>
